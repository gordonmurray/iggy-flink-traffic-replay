# Optional. Defaults produce a 400-request capture and two ~10-second HTTP runs.
REQUEST_COUNT=400
TRAFFIC_RATE_RPS=40
TRAFFIC_SEED=42
FLINK_UI_PORT=8081
# make sets these automatically. Set them for direct Compose use if your IDs differ.
# LOCAL_UID=1000
# LOCAL_GID=1000
